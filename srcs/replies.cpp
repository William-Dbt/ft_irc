#include <string>

std::string& RPL_WELCOME(std::string& prefix) { return ("Welcome to the Internet Relay Network " + prefix); }
// std::string& RPL_YOURHOST() { return ("Your host is <servername>, running version <ver>"); }
